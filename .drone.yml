---
kind: pipeline
type: docker
name: default-build

steps:
  - name: build
    image: golang:1.14.2
    commands:
      - apt-get update && apt-get install -y cmake
      - make test
      - ./contrib/build.sh
  - name: publish-test
    image: plugins/docker:18
    settings:
      dockerfile: docker/Dockerfile.test
      repo: rocketprotocol/rocketzone-relayer-test
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      tags:
        - latest
    when:
      event:
        - push
      branch: ibc
  - name: deploy-devnet
    image: appleboy/drone-scp
    settings:
      username: ubuntu
      host:
        from_secret: devnet_host
      key:
        from_secret: deploy_key
      target:
        from_secret: deploy_home
      source:
        - bin/
    when:
      event:
        - push
      branch: master
  - name: deploy-devnet-srv
    image: appleboy/drone-ssh
    settings:
      username: ubuntu
      host:
        from_secret: devnet_host
      key:
        from_secret: deploy_key
      target:
        from_secret: deploy_home
      script:
        - sh ~/deploy-devnet.sh
    when:
      event:
        - push
      branch: master
  - name: slack
    image: plugins/slack
    settings:
      webhook:
        from_secret: slack_webhook
    when:
      status: [success, failure]

---
kind: pipeline
type: docker
name: integration

steps:
  - name: gaiad
    image: golang:1.14.2
    detach: true
    commands:
      - ./contrib/ci/setup-gaia.sh
  - name: stakebird
    image: golang:1.14.2
    commands:
      - ./contrib/ci/setup-stakebird.sh
    detach: true
  - name: relayer
    image: golang:1.14.2
    commands:
      - /bin/bash ./contrib/ci/setup-relayer.sh

trigger:
  branch:
    - jhernandezb/integration-test

depends_on:
  - default-build
---
kind: signature
hmac: 53b26cc9d9cf0d00d1e515a07ecf2a98ee04bc078b008f1283db5280c2147b53

...
